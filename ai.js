//Handles the ai logic of the the game

const Ai = (() => {

    const smartChoice = () => {

    };

    const randomMoves = () => {

    };

    return {smartChoice, randomMoves};
})();

export default Ai;
